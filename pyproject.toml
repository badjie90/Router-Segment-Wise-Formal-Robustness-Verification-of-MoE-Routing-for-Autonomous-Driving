[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bdd100k-gate-segment-verification"
version = "0.1.0"
description = "Segment-wise empirical and formal robustness analysis for a BDD100K MoE gate"
requires-python = ">=3.11,<3.12"

[tool.setuptools.packages.find]
include = ["gateverify*"]
